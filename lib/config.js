var half_hour = 0.5 // taken in hours

module.exports = {
  default_db_name: 'feeds.db',
  default_interval: half_hour,
  default_email_interval: half_hour * 2,
  torrentfish_config_file: '.torrentfish.js'
}
